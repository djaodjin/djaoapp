# Direct known Python requirements
#
# Note that you need to install native prerequisites first
# (i.e. requirements-native.txt)
#
# (Javascript prerequisites are specified in package.json)

boto3==1.42.89                    # 1.33.13 last version compatible with Py3.7
Django==5.2.14
django-phonenumber-field==7.1.0
django-recaptcha==4.0.0           # 4.0.0 renamed namespace 'django_recaptcha'
                                  # 2.0.6 requires Django<4.0
djangorestframework==3.16.1       # 3.16 requires Python>=3.9
                                  # 3.15 requires Django>=4.2, Python>=3.8
                                  # 3.13.1 is last version supporting Django 2.2
# We need a version of django-fernet-fields that supports Django4.2
django-fernet-fields @ git+https://github.com/djaodjin/django-fernet-fields.git@6f261ed465a7fbafa1c41b635f7acd9fd6f3ccca
# next 8 usually installed through source repo directly.
django-csp==4.0
djaodjin-deployutils==0.14.5
djaodjin-extended-templates==0.5.4
djaodjin-multitier==0.3.2
djaodjin-rules==0.4.11
djaodjin-saas==1.2.3
djaodjin-signup==0.11.4
docutils==0.21.2                  # 0.22 is incompatible with Sphinx==8.1.3
                                  # pip does not complain about botocore
drf-spectacular==0.29.0           # to display 'themes' notifications pages
googlemaps==4.10.0
# gunicorn 19.0 is broken (see issue 787)
# gunicorn 19.3.0 has a syntax error in gunicorn/workers/_gaiohttp.py:64
#   but it still seems to work ...
gunicorn==23.0.0                  # requires Py3.7
jinja2==3.1.6
phonenumbers==8.13.17
pydyf==0.12.1                     # 0.12.1 incompatible with WeasyPrint==53.4
PyJWT==2.12.0                     # 2.11.0 supports Python 3.14
                                  # 2.10.0 requires Python>=3.9
                                  # 2.8.0 requires Python>=3.7
pyotp==2.8.0
pytz==2026.1.post1
social-auth-app-django==5.4.3     # 5.5.0 drops support for Django<5.1
                                  # 5.2.0 drops support for Django<3.2
                                  # v1.2.0 does not support Django>=2.1
stripe==12.5.1                    # Stripe>=13 removed error.py
whitenoise==6.4.0
# >=53 requires Pango1.44, AMZLinux2 has 1.42, but on Debian (Dockerfile), if we
# use version 52.5, it leads to `OSError: cannot load library 'pangocairo-1.0'`
WeasyPrint==66.0                  ; python_version < "3.12"
WeasyPrint==68.1                  ; python_version >= "3.12"

# Optional packages
django-storages==1.14.2           # to upload assets to S3

# To run with DEBUG=1
coreapi==2.3.3                    # XXX until we upgrade djaodjin-saas/signup
django-debug-toolbar==5.0.1       ; python_version >= "3.9" and python_version < "3.10"
django-debug-toolbar==6.2.0       ; python_version >= "3.10"
                                  # 6.2.0 requires python>=3.10
                                  # 4.4.6 requires Django>=4.2.9
                                  # 3.4.0 requires Django>=3.2
                                  # 3.2.4 fails with SQLPanel is not scriptable
                                  # 2.2.1 is the last version for Django2.2
                                  # 1.11 does not support Django 2.2
django-extensions==4.1            # 4.1 supports py312-django5.2
                                  # 3.2.0 requires Django>=4.0
# development
Faker==3.0.1
flex==6.14.0
pygments== 2.20.0
pyYAML==6.0.1
Sphinx==8.1.3                     # (2026-05-27) Read-the-docs runs Py3.11
                                  # 5.3.0 last version that supports Py3.7
sphinx_rtd_theme==3.1.0
sphinxcontrib-httpdomain==2.0.0
sphinxcontrib-websupport==2.0.0   # 1.2.4 last version that supports Py3.7
swagger-spec-validator==3.0.3
